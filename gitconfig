[user]
	name = Valentin Serrago
	email = valentin.serrago@gmail.com
[color]
	ui = true
[color "diff"]
	; see 'git help config' /color.diff.<slot> for more options
	meta = cyan
	frag = magenta bold
[core]
	editor = vim
[merge]
	tool = vimdiff
[alias]
	adal = add --all
	amend = commit --amend
	cmv = commit -v
	cmva = !git add --all && git commit -v
	discard = checkout --
	lg = log -p --color-words
	lgg = log --oneline --graph
	st = status
	unstage = reset HEAD
[commit]
	verbose = true
[push]
	default = current
[pull]
	ff = only
[init]
	defaultBranch = master
